### Create a datset
Write a Python script using PySpark to generate a synthetic dataset of 10,000 rows representing [e.g., e-commerce transactions]. It should include columns: transaction_id (string), user_id (int), amount (double), and timestamp (timestamp). Include some intentional null values in the amount column so we can handle them later.

### sample coding propmt
We want to build a real-time monitoring solution for our global delivery fleet. We need to track driver behaviors, calculate delivery delay risks mid-route, and surface high-value packages that are stuck in transit so our operational teams can intervene. We want this built on Databricks.